# Xico 打包配置模板。复制为 scripts/build.env（已 gitignore），make_app.sh 会自动加载。
# 目的：把「许可公钥 / 各类 URL」等易忘的构建变量持久化，杜绝再打出「无法验签许可、
# 激活显示无效」的坏包（0.3.0 b83 的根因就是漏了 XICO_LICENSE_PUBLIC_KEYS）。
#
# 只填非空项即可；已在环境里 export 的同名变量优先，build.env 只补空缺。

# —— 许可验签（必填，release 缺失会 fail-fast）——
# 格式：<keyID>:<base64 公钥>。keyID 必须与官网 .env 的 XICO_LICENSE_KEY_ID 一致；
# 公钥必须由官网 .env 的 XICO_LICENSE_PRIVATE_KEY 派生（同一对密钥），否则激活必失败。
# 派生方法：在持有私钥的机器上运行 `node scripts/print_license_pubkey.mjs`，把输出粘到这里。
XICO_LICENSE_PUBLIC_KEYS=xico-license-1:REPLACE_WITH_DERIVED_PUBLIC_KEY

# —— 在线激活 / 购买 / 更新源（可选，缺省即下方生产地址）——
XICO_ACTIVATION_URL=https://mac.xicoai.com
XICO_PURCHASE_URL=https://mac.xicoai.com/buy
XICO_FEED_URL=https://mac.xicoai.com/appcast.xml

# —— 规则库在线更新（可选）——
# XICO_DEFINITIONS_URL=
# XICO_DEFINITIONS_PUBLIC_KEYS=
